import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.arise.system',
  appName: 'SoloArise:DailyTracker',
  webDir: 'dist'
};

export default config;
